<% if(error != "") { %>
    <div class="alert alert-danger" role="alert">
        <%= error %>
    </div>
<% } %>

<% if(errorMsg != "") { %>
    <div class="alert alert-danger" role="alert">
        <%= errorMsg %>
    </div>
<% } %>

<% if(successMsg != "") { %>
    <div class="alert alert-success" role="alert">
        <%= successMsg %>
    </div>
<% } %>